apiVersion: v1
kind: Service

metadata:
  name: prediction-dashboard-service

spec:
  type: NodePort

  selector:
    app: prediction-dashboard

  ports:
    - protocol: TCP
      port: 8051
      targetPort: 8051
      nodePort: 30002