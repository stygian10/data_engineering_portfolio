# Week 6 – Data Lake + Visualization Layer

This project introduces:

- Object storage using MinIO (S3-compatible)
- Production-style data lake structure
- Interactive dashboard (Dash)
- Optional Airflow integration

Architecture:

Spark ETL → MinIO (processed/weather) → Dash